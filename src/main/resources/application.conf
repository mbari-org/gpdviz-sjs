akka {
  loglevel = WARNING # DEBUG
}

gpdviz {
  http {
    interface = "0.0.0.0"
    port = 5050
    port = ${?GPDVIZ_PORT}
  }

  async {
    pusher {
      appId   = ${?PUSHER_APPID}
      key     = ${?PUSHER_KEY}
      secret  = ${?PUSHER_SECRET}
    }
  }
}
